services:
  postgres:
    image: postgres:16
    container_name: psychapp-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: psychapp
    ports:
      - "5432:5432"
    volumes:
      - psychapp_pgdata:/var/lib/postgresql/data

volumes:
  psychapp_pgdata:
